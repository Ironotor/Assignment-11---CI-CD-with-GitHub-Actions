name: Echo time
on: 
  workflow_dispatch:
  push: 
  schedule: 
    - cron: "* * * * *"
jobs:
  automated_test_1:
    name: Echo time
    runs-on: ubuntu-latest
    steps:
      - name: Show repository info
        run: echo $(date)

