on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:
jobs:
  automated_test_1:
    name: Automated Test 1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: output debug
      run: echo test
